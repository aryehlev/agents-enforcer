[package]
name = "agent-gateway-enforcer-common"
version = "0.1.0"
edition = "2021"
description = "Shared types for agent-gateway-enforcer eBPF and userspace"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }

[lib]
path = "src/lib.rs"
